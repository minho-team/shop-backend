<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shop.mapper.user.CommentMapper">

    <!-- 문의글에 달린 답변 목록 조회 (삭제되지 않은 것만, 작성순) -->
    <select id="getCommentsByInquiryNo" resultType="Comment">
        SELECT inquiry_comment_no AS inquiryCommentNo,
               inquiry_no         AS inquiryNo,
               member_no          AS memberNo,
               content,
               created_at         AS createdAt,
               updated_at         AS updatedAt,
               delete_yn          AS deleteYn
        FROM inquiry_comment
        WHERE inquiry_no = #{inquiryNo}
          AND delete_yn = 'N'
        ORDER BY created_at ASC
    </select>


</mapper>