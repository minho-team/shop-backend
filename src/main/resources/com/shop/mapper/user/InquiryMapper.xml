<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.user.InquiryMapper">

    <!-- 문의 작성 -->
    <insert id="createInquiry">
        <selectKey keyProperty="inquiryNo" resultType="Long" order="BEFORE">
            SELECT seq_inquiry.nextval FROM dual
        </selectKey>
        INSERT INTO inquiry (
            inquiry_no, member_no, category, title, content, secret_yn, status, created_at, delete_yn
        ) VALUES (
            #{inquiryNo}, #{memberNo}, #{category}, #{title}, #{content}, #{secretYn}, '답변대기', SYSTIMESTAMP, 'N'
        )
    </insert>

    <!-- 내 문의 목록 조회 -->
    <select id="readMyInquiry" resultType="Inquiry">
        SELECT inquiry_no AS inquiryNo,
               member_no  AS memberNo,
               category,
               title,
               content,
               secret_yn  AS secretYn,
               status,
               view_count AS viewCount,
               created_at AS createdAt,
               updated_at AS updatedAt,
               delete_yn  AS deleteYn
        FROM inquiry
        WHERE delete_yn = 'N'
          AND member_no = #{memberNo}
        ORDER BY created_at DESC
    </select>

    <!-- 문의 단건 조회 -->
    <select id="readOneInquiry" resultType="Inquiry">
        SELECT i.inquiry_no AS inquiryNo,
               i.member_no  AS memberNo,
               m.member_id  AS memberId,
               m.name       AS memberName,
               i.category,
               i.title,
               i.content,
               i.secret_yn  AS secretYn,
               i.status,
               i.view_count AS viewCount,
               i.created_at AS createdAt,
               i.updated_at AS updatedAt,
               i.delete_yn  AS deleteYn
        FROM inquiry i
        JOIN member m ON i.member_no = m.member_no
        WHERE i.inquiry_no = #{inquiryNo}
          AND i.delete_yn = 'N'
    </select>

    <!-- 조회수 1 증가 -->
    <update id="increaseViewCount">
        UPDATE inquiry SET view_count = view_count + 1
        WHERE inquiry_no = #{inquiryNo}
    </update>

    <!-- 문의 수정 -->
    <update id="updateInquiry">
        UPDATE inquiry
        SET category   = #{dto.category},
            title      = #{dto.title},
            content    = #{dto.content},
            secret_yn  = #{dto.secretYn},
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
    </update>

    <!-- 문의 삭제 (본인만, soft delete) -->
    <update id="deleteInquiry">
        UPDATE inquiry
        SET delete_yn  = 'Y',
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
          AND member_no  = #{memberNo}
    </update>

    <!-- 내 문의 페이징 조회 -->
    <select id="getMyInquiryPage" resultType="Inquiry">
        SELECT *
        FROM (
            SELECT a.*, ROWNUM AS rnum
            FROM (
                SELECT inquiry_no AS inquiryNo,
                       member_no  AS memberNo,
                       category,
                       title,
                       content,
                       secret_yn  AS secretYn,
                       status,
                       view_count AS viewCount,
                       created_at AS createdAt,
                       updated_at AS updatedAt,
                       delete_yn  AS deleteYn
                FROM inquiry
                WHERE delete_yn = 'N'
                  AND member_no = #{memberNo}
                ORDER BY created_at DESC
            ) a
            WHERE ROWNUM &lt;= #{endRow}
        )
        WHERE rnum &gt; #{startRow}
    </select>

    <!-- 내 문의 건수 조회 (페이징 계산용) -->
    <select id="countMyInquiry" resultType="int">
        SELECT COUNT(*)
        FROM inquiry
        WHERE delete_yn = 'N'
          AND member_no = #{memberNo}
    </select>

    <!--
        문의 상태 변경
        [호출 위치] CommentServiceImpl.createComment() - 답변 작성 시 '답변완료'로 변경
                   CommentServiceImpl.deleteComment()  - 답변 전부 삭제 시 '답변대기'로 복구
        [주의] 이 SQL이 없으면 답변은 저장되지만 updateStatus에서 예외 발생 → 400 반환
    -->
    <update id="updateStatus">
        UPDATE inquiry
        SET status     = #{status},
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
    </update>

</mapper>