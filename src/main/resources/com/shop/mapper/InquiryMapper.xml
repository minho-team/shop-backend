<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shop.mapper.InquiryMapper">

    <!-- 게시글 작성 - 시퀀스로 inquiry_no 채번 후 INSERT -->
    <insert id="createInquiry">
        <selectKey keyProperty="inquiryNo" resultType="Long" order="BEFORE">
            SELECT seq_inquiry.nextval FROM dual
        </selectKey>
        INSERT INTO inquiry (
            inquiry_no, member_no, category, title, content, secret_yn, status, created_at, delete_yn
        )
        VALUES (
            #{inquiryNo}, #{memberNo}, #{category}, #{title}, #{content}, #{secretYn}, '답변대기', SYSTIMESTAMP, 'N'
        )
    </insert>

    <!-- 전체 문의 목록 조회 (관리자용) - 삭제되지 않은 글만, 최신순 정렬 -->
    <select id="readAllInquiry" resultType="Inquiry">
        SELECT inquiry_no   AS inquiryNo,
               member_no    AS memberNo,
               category,
               title,
               content,
               secret_yn    AS secretYn,
               status,
               view_count   AS viewCount,
               created_at   AS createdAt,
               updated_at   AS updatedAt,
               delete_yn    AS deleteYn
        FROM inquiry
        WHERE delete_yn = 'N'
        ORDER BY created_at DESC
    </select>

    <!-- 내 문의 목록 조회 - 로그인한 회원의 글만, 최신순 정렬 -->
    <select id="readMyInquiry" resultType="Inquiry">
        SELECT inquiry_no   AS inquiryNo,
               member_no    AS memberNo,
               category,
               title,
               content,
               secret_yn    AS secretYn,
               status,
               view_count   AS viewCount,
               created_at   AS createdAt,
               updated_at   AS updatedAt,
               delete_yn    AS deleteYn
        FROM inquiry
        WHERE delete_yn = 'N'
          AND member_no = #{memberNo}
        ORDER BY created_at DESC
    </select>

    <!-- 문의 단건 조회 - 삭제되지 않은 글만 -->
    <select id="readOneInquiry" resultType="Inquiry">
        SELECT inquiry_no   AS inquiryNo,
               member_no    AS memberNo,
               category,
               title,
               content,
               secret_yn    AS secretYn,
               status,
               view_count   AS viewCount,
               created_at   AS createdAt,
               updated_at   AS updatedAt,
               delete_yn    AS deleteYn
        FROM inquiry
        WHERE inquiry_no = #{inquiryNo}
          AND delete_yn = 'N'
    </select>

    <!-- 조회수 1 증가 - 게시글 상세 조회 시 호출 -->
    <update id="increaseViewCount">
        UPDATE inquiry
        SET view_count = view_count + 1
        WHERE inquiry_no = #{inquiryNo}
    </update>

    <!-- 문의 수정 - 카테고리, 제목, 내용, 비밀글 여부 변경 -->
    <update id="updateInquiry">
        UPDATE inquiry
        SET category   = #{dto.category},
            title      = #{dto.title},
            content    = #{dto.content},
            secret_yn  = #{dto.secretYn},
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
    </update>

    <!-- 문의 삭제 (soft delete) - delete_yn을 Y로 변경, 본인 글만 삭제 가능 -->
    <update id="deleteInquiry">
        UPDATE inquiry
        SET delete_yn  = 'Y',
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
          AND member_no  = #{memberNo}
    </update>

    <!-- 문의 삭제 (관리자 전용, soft delete) - memberNo 체크 없이 모든 문의 삭제 가능 -->
    <update id="adminDeleteInquiry">
        UPDATE inquiry
        SET delete_yn  = 'Y',
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
    </update>

    <!-- 문의 상태 변경 - 답변 등록/삭제 시 자동 업데이트 (답변대기 / 답변완료) -->
    <update id="updateStatus">
        UPDATE inquiry
        SET status     = #{status},
            updated_at = SYSTIMESTAMP
        WHERE inquiry_no = #{inquiryNo}
    </update>

</mapper>