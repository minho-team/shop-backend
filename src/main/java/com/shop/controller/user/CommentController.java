package com.shop.controller.user;

// 이 파일은 현재 사용하지 않습니다.
// 답변(댓글) 기능은 관리자 전용이므로 AdminCommentController(/api/admin/comment)에서 처리합니다.
// 사용자가 답변을 조회하는 기능은 InquiryController의 readOneInquiry에 포함되어 있습니다.
// (GET /api/inquiry/{inquiryNo} → inquiry + files + comments 를 함께 반환)
public class CommentController {
}
